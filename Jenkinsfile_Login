pipeline {
    
    agent any
        environment {
        // Set the path to your Python installation and virtual environment
        VENV_PATH = 'C:\\Users\\STEL\\venv'
        PATH = "${VENV_PATH}\\Scripts;${env.PATH}"
    }
    
    stages {
        stage('Checkout') {
            steps {
                git branch: 'main', credentialsId: '6314ed17-e1f7-4c32-a229-25fbc493a2dc', url: 'https://github.com/thanhlamha/Stel.git'
            }
        }
            
        // stage('Install dependencies') {
        //     steps {
        //         bat 'pip install -r requirements.txt'
        //     }
        // }

        stage('Run tests') {
            steps {
                bat 'robot --outputdir results --include %TAGS% tests\\login\\login.robot'
            }
        }
        
    }
    
    post {
        success {
            echo 'Tests passed - deployment can proceed'
        }
        
        failure {
            echo 'Tests failed - deployment halted'
        }
        
        // always {
        //     script {
        //         // Ensure the cleanup command is executable
        //         if (fileExists('venv')) {
        //             bat 'rm -rf venv'
        //         } else {
        //             echo 'Virtual environment not found; skipping cleanup'
        //         }
        //     }
        // }
    }
}
